'use client';
import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {Bell,ChevronDown,Grid2X2,Menu,Package,Settings,ShoppingCart,Users,X,BarChart3,Layers3} from 'lucide-react'; import {useState} from 'react';
const items=[['','Dashboard',Grid2X2],['users','Users',Users],['products','Products',Package],['orders','Orders',ShoppingCart],['categories','Categories',Layers3],['customers','Customers',Users],['reports','Reports',BarChart3],['settings','Settings',Settings]] as const;
export default function AdminLayout({children}:{children:React.ReactNode}){const path=usePathname();const[open,setOpen]=useState(false);return <div className="admin-frame"><aside className={`admin-sidebar ${open?'is-open':''}`}><div className="brand"><span className="brand-mark">N</span><span>NH MART</span><button className="sidebar-close" onClick={()=>setOpen(false)}><X size={18}/></button></div><div className="sidebar-label">MENU</div><nav>{items.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} className={path===`/admin/${href}`?'active':''} href={`/admin/${href}`} key={label}><Icon size={17}/><span>{label}</span>{label==='Dashboard'&&<span className="nav-dot"/>}</Link>)}</nav><div className="sidebar-bottom"><div className="upgrade"><span>Pro workspace</span><small>Unlock advanced reports</small><button>Upgrade plan</button></div><div className="sidebar-user"><span className="avatar">AK</span><span><b>Admin User</b><small>Super admin</small></span><ChevronDown size={15}/></div></div></aside>{open&&<button className="sidebar-overlay" onClick={()=>setOpen(false)} aria-label="Close menu"/>}<section className="admin-content"><header className="admin-topbar"><button className="mobile-menu" onClick={()=>setOpen(true)}><Menu size={21}/></button><div className="breadcrumbs"><span>Workspace</span><b>/</b><strong>Overview</strong></div><div className="top-actions"><button className="icon-btn"><Bell size={19}/><i/></button><span className="top-divider"/><span className="top-avatar">AK</span><span className="top-name">Admin User</span><ChevronDown size={15}/></div></header><main className="admin-main">{children}</main></section></div>}
